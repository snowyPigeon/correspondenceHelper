// Get the greetings and endings
function getRecommendations() {
	// query the relevant tables for greeting, title, firstName, surname and ending
	// getElementById("recommendations").innerHTML += "<p>You chose</p>"; K to replace
}

// Get the chosen context
function getContext() {
	
}

// Set the chosen context
function toggleBusiness() {
	/* if 	(document.getElementById("business").value = false) { K to replace
		document.getElementById("business").value = true;
	}
	else
	{
		document.getElementById("business").value = false;
	} */
}

// Get the chosen style
function getStyle() {
	
}

// Set the chosen style
function setStyle() {
	
}

// Get the title
function getTitle() {
	
}

// Set the title
function setTitle() {
	
}

// Event listeners
document.getElementById("business").addEventListener("click", 
toggleBusiness);

